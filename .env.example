# 복사해서 .env로 저장 후 값 채우기 (.env는 gitignore됨)
UNI_RAG_BASE_URL=http://221.147.100.161:8000
UNI_RAG_MODEL_KEY=qwen3-coder-next
# /chat/ 생성 길이 상한 (0이면 미전송 — 서버 기본값 사용)
LLM_MAX_TOKENS=4096
HOST=127.0.0.1
PORT=8080

# 통합 테스트 전용 (선택) — 앱 자체는 로그인 화면에서 입력받는다
TEST_UNE_ACCOUNT=
TEST_UNE_PASSWORD=
